export enum DiffChunkType {
  ADDED = 'added',
  RENAMED = 'renamed',
  CHANGED = 'changed',
  DELETED = 'deleted',
}

export enum DiffLineType {
  ADDED = 'added',
  DELETED = 'deleted',
  NORMAL = 'normal',
}
